name: Verify Artifacts

on:
  pull_request:
  push:
    branches:
      - main
      - dev

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install ripgrep if missing
        run: |
          if ! command -v rg >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y ripgrep
          fi

      - name: Verify documentation artifacts
        run: bash scripts/verify_artifacts.sh

      - name: Verify docs site config
        run: |
          python - <<'PY'
          from pathlib import Path

          required = [
              "mkdocs.yml",
              "docs/index.md",
              "docs/00-start-here/knowledge-map.md",
              "docs/00-start-here/docs-site-guide.md",
              "docs/00-start-here/version-manifest.md",
              ".github/workflows/deploy-docs-pages.yml",
          ]
          for path in required:
              if not Path(path).exists():
                  raise SystemExit(f"missing {path}")
          PY
